package com.ming.blog.service;

import org.springframework.stereotype.Service;

/**
 * @author Jiang Zaiming
 * @date 2020/4/3 11:44 上午
 */
@Service
public class PermissionService {

//    @Autowired
//    private PermissionDao permissionDao;
//
//    public List<Permission> getPermissionsByRoles(List<Role> roles) {
//        List<Integer> roleIdList = roles.stream().map(Role :: getId).collect(Collectors.toList());
//        return permissionDao.getPermissionsByRoles(roleIdList);
//    }
//
//
//    public List<Permission> getByRole(Role role) {
//        return permissionDao.getByRole(role.getId());
//    }
}
